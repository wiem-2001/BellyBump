<form method="post" action="{{ path('app_etab_delete', {'id': etab.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etab.id) }}">
    <button class="btn">Delete</button>
</form>
