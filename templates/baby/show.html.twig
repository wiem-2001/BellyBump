{% extends 'back.html.twig' %}

{% block title %}Baby{% endblock %}

{% block body %}
    <h1>Baby</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ baby.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ baby.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ baby.prenom }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ baby.sexe }}</td>
            </tr>
            <tr>
                <th>DateNaissance</th>
                <td>{{ baby.dateNaissance ? baby.dateNaissance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ baby.poids }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ baby.taille }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_baby_index') }}">back to list</a>

    <a href="{{ path('app_baby_edit', {'id': baby.id}) }}">edit</a>

    {{ include('baby/_delete_form.html.twig') }}
{% endblock %}
