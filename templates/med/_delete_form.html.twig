<form method="post" action="{{ path('app_med_delete', {'id': med.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ med.id) }}">
    <button class="btn">Delete</button>
</form>
