{% extends 'back.html.twig' %}

{% block title %}Doctors{% endblock %}

{% block body %}
    <h1>Doctors</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ med.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ med.nom }}</td>
            </tr>
            <tr>
                <th>FullName</th>
                <td>{{ med.prenom }}</td>
            </tr>
            <tr>
                <th>Speciality</th>
                <td>{{ med.specialité }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_med_index') }}">back to list</a>

    <a href="{{ path('app_med_edit', {'id': med.id}) }}">edit</a>

    {{ include('med/_delete_form.html.twig') }}
{% endblock %}
