{% extends 'base.html.twig' %}
{% block header %}  {% endblock %}
{% block body %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    <!-- Hero Section - Home Page -->
    <section id="hero" class="hero">
        <img src="frontOffice/assets/img/hero-bg.webp" alt="" style="opacity: 0.5;" >
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-6">
                    <div class="text-center">
                        <strong style="font-size: 30px;">Sign Up Now <br>and <br>Embrace Motherhood with Us</strong>
                    </div>
                    {{ form_start(registrationForm,{'attr': {'novalidate': 'novalidate'}} ) }}
                    {{ form_errors(registrationForm) }}

                    <div style="padding-top: 80px;">
                        {{ form_label(registrationForm.firstName) }}
                        {{ form_widget(registrationForm.firstName, { 'attr': { 'class': 'form-control' ~ (not registrationForm.vars.valid ? ' is-invalid' : ''), 'placeholder': 'Enter your first name'} } )}}
                        <div class="error-message"><Strong>{{ form_errors(registrationForm.firstName) }}</Strong></div>
                    </div>

                    <div>
                        {{ form_label(registrationForm.lastName) }}
                        {{ form_widget(registrationForm.lastName, { 'attr': { 'class': 'form-control' ~ (not registrationForm.vars.valid ? ' is-invalid' : ''), 'placeholder': 'Enter your last name'} } )}}
                        <div class="error-message"><Strong>{{ form_errors(registrationForm.lastName) }}</Strong></div>
                    </div>

                    <div>
                        {{ form_label(registrationForm.email) }}
                        {{ form_widget(registrationForm.email, { 'attr': { 'class': 'form-control' ~ (not registrationForm.vars.valid ? ' is-invalid' : ''), 'placeholder': 'Enter email address' } }) }}
                        <div class="error-message"><Strong>{{ form_errors(registrationForm.email) }}</Strong></div>
                    </div>

                    <div>
                        {{ form_label(registrationForm.plainPassword.first) }}
                        {{ form_widget(registrationForm.plainPassword.first, { 'attr': { 'class': 'form-control' ~ (not registrationForm.vars.valid ? ' is-invalid' : ''), 'placeholder': 'Enter password' } }) }}
                        <div class="error-message"><Strong>{{ form_errors(registrationForm.plainPassword.first) }}</Strong></div>
                    </div>

                    <div>
                        {{ form_label(registrationForm.plainPassword.second) }}
                        {{ form_widget(registrationForm.plainPassword.second, { 'attr': { 'class': 'form-control' ~ (not registrationForm.vars.valid ? ' is-invalid' : ''), 'placeholder': 'Confirm password' } }) }}
                        <div class="error-message"><Strong>{{ form_errors(registrationForm.plainPassword.second) }}</Strong></div>
                    </div>

                    <div>
                        {{ form_label(registrationForm.birthday) }}
                        {{ form_widget(registrationForm.birthday) }}
                        <div class="error-message"><Strong>{{ form_errors(registrationForm.birthday) }}</Strong></div>
                    </div>

                   <div style="margin-top: 10px;">
                      {{ form_widget(registrationForm.save, {'label': 'Sign up', 'attr': {'class': 'btn btn-danger'}}) }}
                    </div>

                    {{ form_end(registrationForm) }}
                </div>
            </div>
        </div>
    </section><!-- End Hero Section -->

    <style>
        .error-message {
            color: #AB3319;
            font-size: 14px;
            margin-top: 5px;
        }
        .is-invalid {
            border-color: #AB3319;
        }
    </style>
{% endblock %}
